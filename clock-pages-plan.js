(function(App){
'use strict';
const {Core,money,esc,dateBR,percent,state,currentMonth}=App;
App.renderers.budget=()=>{const data=state(),month=currentMonth(),spending=Core.categorySpending(month),budgets=data.budgets.filter(b=>b.month===month);return`<div class="section-head"><button class="btn primary" data-action="set-budget">Definir orçamento</button><strong>Resultado: ${money.format(Core.monthSummary(month).budgetResult)}</strong></div><div class="cards-grid">${budgets.length?budgets.map(b=>{const used=spending.get(b.category)||0,pct=b.limit?Math.min(100,used/b.limit*100):0;return`<div class="entity-card"><h3>${esc(b.category)}</h3><div class="big ${used>b.limit?'red':''}">${money.format(used)}</div><small class="muted">de ${money.format(b.limit)}</small><div class="progress"><i style="width:${pct}%;background:${used>b.limit?'var(--red)':'var(--brand)'}"></i></div><strong>${percent(pct)} usado</strong></div>`}).join(''):'<div class="empty">Defina limites para as categorias deste mês.</div>'}</div><div class="card" style="margin-top:12px"><h3>Gastos por categoria</h3><div class="list">${[...spending.entries()].sort((a,b)=>b[1]-a[1]).map(([c,v])=>`<div class="item"><div class="item-main"><strong>${esc(c)}</strong></div><strong>${money.format(v)}</strong></div>`).join('')||'<div class="empty">Sem gastos no período.</div>'}</div></div>`};
App.renderers.goals=()=>{const data=state();return`<div class="section-head"><button class="btn primary" data-action="add-goal">Nova meta</button><span class="muted">Aporte reduz a conta e aumenta a meta, sem reduzir o patrimônio.</span></div><div class="cards-grid">${data.goals.length?data.goals.map(g=>{const current=Core.goalCurrent(g.id),pct=g.target?Math.min(100,current/g.target*100):0;return`<div class="entity-card"><small class="muted">${esc(g.category)} ${g.deadline?`· até ${dateBR(g.deadline)}`:''}</small><h3>${esc(g.name)}</h3><div class="big">${money.format(current)}</div><small class="muted">Meta ${money.format(g.target)}</small><div class="progress"><i style="width:${pct}%"></i></div><button class="btn primary" data-action="goal-contribution" data-id="${g.id}">Fazer aporte</button></div>`}).join(''):'<div class="empty">Nenhuma meta cadastrada.</div>'}</div>`};
App.renderers.debts=()=>{const data=state();return`<div class="section-head"><button class="btn primary" data-action="add-debt">Nova dívida</button><strong>Total devedor: ${money.format(Core.netWorth().debts)}</strong></div><div class="cards-grid">${data.debts.length?data.debts.map(d=>{const balance=Core.debtBalance(d.id),paid=Math.max(0,d.originalBalance-balance),pct=d.originalBalance?Math.min(100,paid/d.originalBalance*100):0;return`<div class="entity-card"><small class="muted">Taxa ${d.interestRate||0}% · vence dia ${d.dueDay}</small><h3>${esc(d.name)}</h3><div class="big red">${money.format(balance)}</div><small class="muted">Original ${money.format(d.originalBalance)}</small><div class="progress"><i style="width:${pct}%;background:var(--green)"></i></div><button class="btn success" data-action="pay-debt" data-id="${d.id}" ${balance<=0?'disabled':''}>Registrar pagamento</button></div>`}).join(''):'<div class="empty">Nenhuma dívida cadastrada.</div>'}</div>`};
App.renderers.investments=()=>{const data=state();return`<div class="section-head"><button class="btn primary" data-action="add-investment">Novo investimento</button><strong>Total investido: ${money.format(Core.netWorth().investments)}</strong></div><div class="cards-grid">${data.investments.length?data.investments.map(i=>`<div class="entity-card"><small class="muted">${esc(i.type)} · risco ${esc(i.risk)} · ${esc(i.liquidity)}</small><h3>${esc(i.name)}</h3><div class="big green">${money.format(Core.investmentCurrent(i.id))}</div><div class="badge-row">${i.emergencyReserve?'<span class="pill paid">Reserva de emergência</span>':''}</div><button class="btn primary" data-action="investment-contribution" data-id="${i.id}">Fazer aporte</button></div>`).join(''):'<div class="empty">Nenhum investimento cadastrado.</div>'}</div>`};
App.renderers.assets=()=>{const data=state(),worth=Core.netWorth();return`<div class="metrics"><div class="card"><small class="muted">Dinheiro em contas</small><div class="metric-value">${money.format(worth.cash)}</div></div><div class="card"><small class="muted">Metas e reservas</small><div class="metric-value">${money.format(worth.goals)}</div></div><div class="card"><small class="muted">Investimentos</small><div class="metric-value">${money.format(worth.investments)}</div></div><div class="card"><small class="muted">Patrimônio líquido total</small><div class="metric-value">${money.format(worth.total)}</div></div></div><div class="section-head" style="margin-top:12px"><button class="btn primary" data-action="add-asset">Novo bem</button><span class="muted">Dívidas são descontadas automaticamente.</span></div><div class="cards-grid">${data.assets.map(a=>`<div class="entity-card"><small class="muted">${esc(a.type)}</small><h3>${esc(a.name)}</h3><div class="big">${money.format(a.value)}</div></div>`).join('')||'<div class="empty">Nenhum bem cadastrado.</div>'}</div>`};
App.renderers.health=()=>{const h=Core.health(currentMonth()),s=h.summary,w=h.worth,statusClass=h.label==='Em dificuldade'?'danger':h.label.includes('instável')?'warning':'',savingsRate=s.income?s.savings/s.income*100:0;return`<div class="card status-banner ${statusClass}"><h2>${h.label}</h2><p class="muted">A classificação muda automaticamente com saldo, orçamento, reserva e dívida.</p></div><div class="metrics"><div class="card"><small class="muted">Orçamento</small><div class="metric-value ${s.budgetResult<0?'red':'green'}">${s.budgetResult<0?'Deficitário':'Superavitário'}</div><div class="kpi-note">${money.format(s.budgetResult)}</div></div><div class="card"><small class="muted">Reserva</small><div class="metric-value">${h.reserveMonths.toFixed(1)} mês(es)</div><div class="kpi-note">${money.format(h.reserve)}</div></div><div class="card"><small class="muted">Comprometimento com dívidas</small><div class="metric-value">${percent(h.debtRatio)}</div></div><div class="card"><small class="muted">Taxa de aportes</small><div class="metric-value">${percent(savingsRate)}</div></div></div><div class="grid equal"><div class="card"><h3>Leitura automática</h3><div class="list"><div class="item"><div class="item-main"><strong>Receitas e gastos</strong><small>${money.format(s.income)} de entradas e ${money.format(s.spending)} em consumo.</small></div></div><div class="item"><div class="item-main"><strong>Proteção</strong><small>Meta recomendada: ${state().settings.emergencyMonths||6} meses de despesas essenciais.</small></div></div><div class="item"><div class="item-main"><strong>Patrimônio</strong><small>${money.format(w.total)} após descontar ${money.format(w.debts)} em dívidas.</small></div></div></div></div><div class="card"><h3>Próxima prioridade</h3><p>${s.budgetResult<0?'Reduzir despesas ou aumentar receitas até o orçamento voltar a ser superavitário.':h.reserveMonths<3?'Fortalecer a reserva de emergência.':w.debts>0?'Acelerar a quitação das dívidas.':'Manter aportes e diversificação de longo prazo.'}</p></div></div>`};
})(window.MVFApp);
