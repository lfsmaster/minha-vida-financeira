(function(App){
'use strict';
const {Core,money,esc,dateBR,state,currentMonth}=App;
function hero(){const data=state(),balances=Core.balances(),available=Core.totalAvailable(),projected=Core.projectedAvailable(),imported=data.transactions.filter(t=>t.source==='import'),last=imported.map(t=>t.date).sort().at(-1);return`<section class="hero ${available<0?'negative':''}"><div class="hero-grid"><div><div class="hero-label">SALDO DISPONÍVEL AGORA</div><div class="hero-value">${money.format(available)}</div><div>Saldo real: saldo inicial + entradas realizadas − saídas, pagamentos e aportes realizados.</div><div class="small" style="margin-top:8px;opacity:.85">${imported.length?`${imported.length} lançamento(s) do extrato considerados${last?` · último em ${dateBR(last)}`:''}.`:'Nenhum extrato importado.'}</div></div><div class="hero-projected"><small>Saldo projetado</small><strong>${money.format(projected)}</strong><small>Inclui pendências e faturas abertas. Não confundir com dinheiro disponível.</small></div></div><div class="account-strip">${data.accounts.map(a=>`<div class="account-mini"><span>${esc(a.name)}</span><strong>${money.format(balances.get(a.id)||0)}</strong></div>`).join('')}</div></section>`}
function txItem(t){const sign=t.kind==='income'?'+':t.kind==='transfer'?'↔':'−';return`<div class="item"><div class="item-main"><strong>${esc(t.description)}</strong><small>${dateBR(t.date)} · ${esc(t.category||t.kind)}</small></div><span class="pill ${t.status}">${t.status==='paid'?'Realizado':'Pendente'}</span><span class="amount ${t.kind==='income'?'green':'red'}">${sign} ${money.format(t.amount)}</span></div>`}
App.renderers.dashboard=()=>{const data=state(),month=currentMonth(),summary=Core.monthSummary(month),worth=Core.netWorth(),health=Core.health(month),recent=[...data.transactions].sort((a,b)=>String(b.date).localeCompare(String(a.date))).slice(0,6),pending=data.transactions.filter(t=>t.status==='pending').sort((a,b)=>String(a.date).localeCompare(String(b.date))).slice(0,5),statusClass=health.label==='Em dificuldade'?'danger':health.label.includes('instável')?'warning':'';return`${hero()}<div class="metrics"><div class="card"><small class="muted">Entradas realizadas</small><div class="metric-value green">${money.format(summary.income)}</div></div><div class="card"><small class="muted">Saídas de caixa</small><div class="metric-value red">${money.format(summary.cashExpenses)}</div></div><div class="card"><small class="muted">Aportes para patrimônio</small><div class="metric-value">${money.format(summary.savings)}</div></div><div class="card"><small class="muted">Patrimônio líquido</small><div class="metric-value">${money.format(worth.total)}</div></div></div><div class="card status-banner ${statusClass}" style="margin-top:12px"><strong>Saúde financeira: ${health.label}</strong><p class="muted" style="margin:6px 0 0">Resultado orçamentário ${money.format(summary.budgetResult)} · reserva ${health.reserveMonths.toFixed(1)} mês(es) · dívidas ${money.format(worth.debts)}.</p></div><div class="card" style="margin-top:12px"><h3>Funcionamento em cadeia</h3><div class="clock-chain"><div class="clock-step">1. Lançamento ou extrato</div><div class="clock-step">2. Conta e saldo</div><div class="clock-step">3. Orçamento e faturas</div><div class="clock-step">4. Metas, dívidas e patrimônio</div><div class="clock-step">5. Relatórios e saúde</div></div><p class="kpi-note">Toda alteração passa pelo motor central e recalcula os módulos derivados no mesmo instante.</p></div><div class="grid"><div class="card"><h3>Últimas movimentações</h3><div class="list">${recent.length?recent.map(txItem).join(''):'<div class="empty">Nenhuma movimentação.</div>'}</div></div><div class="card"><h3>Próximos compromissos</h3><div class="list">${pending.length?pending.map(txItem).join(''):'<div class="empty">Nenhuma pendência.</div>'}</div></div></div>`};
App.renderers.transactions=()=>{const data=state(),rows=[...data.transactions].sort((a,b)=>String(b.date).localeCompare(String(a.date)));return`<div class="section-head"><div class="actions"><button class="btn primary" data-action="add-income">Nova receita</button><button class="btn" data-action="add-expense">Nova despesa</button><button class="btn" data-action="transfer">Transferir</button></div><div class="filter-note">Todos os lançamentos alimentam os demais módulos.</div></div><div class="card"><div class="table-wrap"><table><thead><tr><th>Data</th><th>Descrição</th><th>Categoria</th><th>Conta/Origem</th><th>Tipo</th><th>Status</th><th>Valor</th><th>Ações</th></tr></thead><tbody>${rows.map(t=>{const account=data.accounts.find(a=>a.id===(t.accountId||t.fromAccountId));return`<tr><td>${dateBR(t.date)}</td><td>${esc(t.description)}</td><td>${esc(t.category)}</td><td>${esc(account?.name||'—')}</td><td>${esc(t.kind)}</td><td><span class="pill ${t.status}">${t.status==='paid'?'Realizado':'Pendente'}</span></td><td>${money.format(t.amount)}</td><td>${t.status==='pending'?`<button class="link" data-action="settle" data-id="${t.id}">Baixar</button>`:''}<button class="link danger" data-action="delete-tx" data-id="${t.id}">Excluir</button></td></tr>`}).join('')}</tbody></table></div></div>`};
App.renderers.accounts=()=>{const data=state(),balances=Core.balances();return`<div class="section-head"><div class="actions"><button class="btn primary" data-action="add-account">Nova conta</button><button class="btn" data-action="transfer">Transferência</button></div><strong>Total disponível: ${money.format(Core.totalAvailable())}</strong></div><div class="cards-grid">${data.accounts.map(a=>`<div class="entity-card"><small class="muted">${esc(a.type)} ${a.institution?`· ${esc(a.institution)}`:''}</small><h3>${esc(a.name)}</h3><div class="big ${balances.get(a.id)<0?'red':'green'}">${money.format(balances.get(a.id)||0)}</div><small class="muted">Saldo inicial: ${money.format(a.initial)}</small></div>`).join('')}</div>`};
App.renderers.cards=()=>{const data=state();return`<div class="section-head"><div class="actions"><button class="btn primary" data-action="add-card">Novo cartão</button><button class="btn" data-action="card-purchase">Registrar compra</button></div><span class="muted">Compra aumenta a fatura e o orçamento; pagamento reduz a conta.</span></div><div class="cards-grid">${data.cards.length?data.cards.map(card=>{const open=Core.cardOpenTotal(card.id),usage=card.limit?Math.min(100,open/card.limit*100):0;return`<div class="entity-card"><small class="muted">Fecha dia ${card.closingDay} · vence dia ${card.dueDay}</small><h3>${esc(card.name)}</h3><div class="big">${money.format(open)}</div><small class="muted">Fatura aberta · limite ${money.format(card.limit)}</small><div class="progress"><i style="width:${usage}%"></i></div><button class="btn success" data-action="pay-card" data-id="${card.id}" ${open<=0?'disabled':''}>Pagar fatura</button></div>`}).join(''):'<div class="empty">Nenhum cartão cadastrado.</div>'}</div><div class="card" style="margin-top:12px"><h3>Compras recentes</h3><div class="list">${[...data.cardPurchases].sort((a,b)=>String(b.date).localeCompare(String(a.date))).slice(0,10).map(p=>`<div class="item"><div class="item-main"><strong>${esc(p.description)}</strong><small>${dateBR(p.date)} · ${esc(p.category)}</small></div><span class="pill ${p.status}">${p.status==='paid'?'Fatura paga':'Em aberto'}</span><strong>${money.format(p.amount)}</strong></div>`).join('')||'<div class="empty">Nenhuma compra.</div>'}</div></div>`};
App.renderers.bills=()=>{const data=state(),pay=data.transactions.filter(t=>t.status==='pending'&&t.kind==='expense'),receive=data.transactions.filter(t=>t.status==='pending'&&t.kind==='income');return`<div class="grid equal"><div class="card"><div class="section-head"><h3>Contas a pagar</h3><button class="btn" data-action="add-expense">Adicionar</button></div><div class="list">${pay.map(t=>`<div class="item"><div class="item-main"><strong>${esc(t.description)}</strong><small>${dateBR(t.date)} · ${esc(t.category)}</small></div><strong class="red">${money.format(t.amount)}</strong><button class="btn success" data-action="settle" data-id="${t.id}">Pagar</button></div>`).join('')||'<div class="empty">Nada a pagar.</div>'}</div></div><div class="card"><div class="section-head"><h3>Contas a receber</h3><button class="btn" data-action="add-income">Adicionar</button></div><div class="list">${receive.map(t=>`<div class="item"><div class="item-main"><strong>${esc(t.description)}</strong><small>${dateBR(t.date)} · ${esc(t.category)}</small></div><strong class="green">${money.format(t.amount)}</strong><button class="btn success" data-action="settle" data-id="${t.id}">Receber</button></div>`).join('')||'<div class="empty">Nada a receber.</div>'}</div></div></div>`};
})(window.MVFApp);
